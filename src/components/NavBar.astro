<header class="navbar">
    <a href="#inicio" class="logo">MARVEL<span>.</span></a>
    <nav>
        <a href="#inicio">Inicio</a>
        <a href="#peliculas">Películas</a>
        <a href="#estadisticas">Estadísticas</a>
    </nav>
</header>

<style>
    .navbar {
        position: sticky;
        top: 0;
        z-index: 100;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 18px 6%;    
        background: rgba(11, 11, 15, 0.9);
        border-bottom: 1px solid #24242c;
        backdrop-filter: blur(10px);
    }

    .logo {
        color: #ffffff;
        font-size: 1.4rem;
        font-weight: 900;
        letter-spacing: 2px;
        text-decoration: none;
    }

    .logo span {
        color: #e62429;
    }

    nav {
        display: flex;
        gap: 25px;
    }

    nav a {
        color: #cfcfd4;
        font-size: 0.95rem;
        text-decoration: none;
        transition: color 0.2s ease;
    }

    nav a:hover {
        color: #ffffff;
    }

    @media (max-width: 700px) {
        .navbar {
            padding: 15px 5%;
        }

        nav {
            display: none;
        }
    }
</style>